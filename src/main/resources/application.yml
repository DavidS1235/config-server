server:
  port: 8081
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/DavidS1235/config-repository.git
          searchPaths: /
#          username: ${GIT_USER}
#          password: ${GIT_PASS}
#  security:
#    user:
#      name: ds
#      password: ds123